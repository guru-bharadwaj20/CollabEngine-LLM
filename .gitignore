__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Experiment outputs — transcripts are large and regenerable from seeds.
runs/
out/
*.jsonl
*.jsonl.gz
!tests/fixtures/*.jsonl

# Local model weights
models/
*.gguf
*.safetensors

# Vendored llama.cpp binaries and CUDA runtime (~640 MB, fetched by
# docs/LLAMACPP-SETUP.md; regenerable, never committed).
vendor/

# A polluted corpus backup, kept out of git but not deleted (LOG 3.13).
*.polluted.bak
